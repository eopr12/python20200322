# 교재 159-167페이지 키워드 매개변수 설명 잘되있음
# 함수>>모듈(파일),패키지(폴더) 로 이해
# 함수 쓰는 이유:중복 제거,반복작업 제거
# 함수 = 입력과 출력을 수행하는 박스 / 입력은 여러개 출력은 한개(반환) / 입력은 있는데 출력은 없는(반환x) 케이스도 있음


# 어떤 것이 함수인가? 어떻게 함수를 찾을 수 있나
name = input("이름이 무엇인가요?")
print = ("만나서 반갑습니다." + name + "씨!")
age = input("나이는요?")
print("당신은" + age + "살이시군요." + name + "씨!")
